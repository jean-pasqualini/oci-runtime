technical_name: delete-container
description: Add a `delete` subcommand to remove a previously created/stopped container, cleaning up its on-disk state under --root and any leftover infrastructure resources (namespaces, mounts, ipc artifacts).

tasks:
- [x] Wire the `delete` subcommand in `cmd/oci-runtime/` (register in `cmd.go`, hook into `main.go`); accept `--root` and a container name argument, mirroring `create`/`start`/`run` flag conventions.
- [x] Add a delete handler under `internal/app/` that loads the container state from `--root/<name>` and orchestrates teardown via the infrastructure layer.
- [x] Refuse deletion when the container is in a running state (read state, return a clear error); only proceed when stopped/created.
- [x] Implement the real `ContainerStateLoader` in `infrastructure/` and wire it in `main.go` (replacing `stubStateLoader`). Today create/start do NOT persist any status file — the loader's job is to detect existence by checking `--root/<name>/exec.fifo` (folder + fifo present = container exists), and return a clear "container not found" error otherwise. Do NOT grow `domain.ContainerState`; `Status` stays empty because nothing writes it yet.
- [x] Remove the container's state directory `--root/<name>` last, after the loader confirms the container is deletable.
- [x] Refactor `ContainerStateLoader` + `ContainerStateRemover` into a single `ContainerStateManager` port (Load + Remove methods); consolidate `container.Loader` + `container.Remover` into `container.Manager` + `container.NewManager()`, matching the one-Manager-per-infra-package convention (mount, ns, proc...). Update `NewDeleteHandler` signature and `main.go` wiring.
- [ ] Manual verification: create a container, delete it, confirm state dir and resources are gone; then try deleting a running container and confirm the refusal path.

Memory:
- Task 1 (2026-05-15): Registered `delete` subcommand in `cmd/oci-runtime/cmd.go` with `<name>` arg and required `--root` flag, mirroring `create` flag style (local required flag rather than relying on the global one). Action is a stub returning `cli.Exit("delete not implemented yet", 2)`; wiring through the `Actions` struct + `main.go` is deferred to task 2, when `app.DeleteCmd` / `NewDeleteHandler` exist. Build verified via `go run -tags medium ./cmd/oci-runtime check`.
- Task 2 (2026-05-18): Added `app.DeleteCmd{Name, MetadataRoot}` + `app.NewDeleteHandler(ContainerStateLoader)` in `internal/app/delete.go`. New port `ContainerStateLoader` in `ports.go` returning `domain.ContainerState` (minimal struct `{Name, Status}` created at `internal/domain/container_state.go`; will grow in tasks 3+). Handler loads state via port, wraps load error with `xerr.Op`, leaves teardown as TODOs for tasks 3-6. Wired into `Actions.Delete` + `main.go`; cmd.go stub replaced with real action call. main.go injects an inline `stubStateLoader` returning `errors.New("state loader not implemented (task 4)")` — task 4 must replace it with a real infra impl.
- Task 3 (2026-05-18): Added `domain.StatusRunning = "running"` constant in `internal/domain/container_state.go` (only constant needed for the refusal path; create/stopped strings deferred until a real persister exists). Bound the previously-discarded state in `delete.go` and inserted a check before the teardown TODOs: when `state.Status == domain.StatusRunning`, return `xerr.Op("refuse delete", errors.New("container is running"), KV{name, status})`. Status comparison is real but unreachable end-to-end until task 4 replaces the `stubStateLoader`. Build verified via `go run -tags medium ./cmd/oci-runtime check`.
- Task 4 (2026-05-18): Added `internal/infrastructure/container/state.go` with `container.Loader` implementing `app.ContainerStateLoader`. `Load` stats `<root>/<name>/exec.fifo`: missing → wrapped "container not found" via `xerr.Op` with KV{root, name, exec_fifo}; other stat err → wrapped "stat exec fifo"; success → `domain.ContainerState{Name: name}` (Status left empty — nothing persists it). Wired in `main.go` via `container.NewLoader()`, replacing `stubStateLoader` (struct + `errors`/`domain` imports removed). Build verified via `go run -tags medium ./cmd/oci-runtime check`. Refusal check from task 3 remains dead code in practice — loader never returns a non-empty Status.
- Task 5 (2026-05-18): Added `ContainerStateRemover` port in `internal/app/ports.go` (`Remove(ctx, root, name) error`). Added `container.Remover` + `container.NewRemover()` in `internal/infrastructure/container/state.go` doing `os.RemoveAll(<root>/<name>)` wrapped with `xerr.Op("remove state dir", ..., KV{path})`. `NewDeleteHandler` signature grew to `(ContainerStateLoader, ContainerStateRemover)`; handler now calls `remover.Remove` after the refusal check and logs `container deleted` at Info. Wired in `main.go` as `app.NewDeleteHandler(container.NewLoader(), container.NewRemover())`. Build verified via `go run -tags medium ./cmd/oci-runtime check`. Kept Loader and Remover as two separate structs/ports (rather than one `container.Manager`) to minimize churn; consolidation is a future refactor if desired.
- Task 6 (2026-05-18): Consolidated per request before middleware step. `ports.go`: merged `ContainerStateLoader` + `ContainerStateRemover` into single `ContainerStateManager` (Load + Remove). `internal/infrastructure/container/state.go`: replaced `Loader`/`Remover` structs with one `Manager` + `NewManager()` carrying both methods (matches `mount.Manager`, `ns.Manager`, etc.). `delete.go`: handler field reduced to single `state ContainerStateManager`; `NewDeleteHandler(ContainerStateManager)` single-arg again. `main.go`: `app.NewDeleteHandler(container.NewManager())`. Build verified via `go run -tags medium ./cmd/oci-runtime check`. No behavior change — pure refactor.

how to test:
1. `run` subcommand with `--root /tmp/state --bundle /app/bundle <name>`, stop it, then invoke the new `delete --root /tmp/state <name>` and check `/tmp/state/<name>` is removed and no stale mounts/namespaces remain.
2. `create` a container without starting it, `delete` it, verify clean teardown.
3. Start a container, attempt `delete` while running — expect a non-zero exit with a clear "container is running" error and no state changes.
4. Use the `check` subcommand before/after to confirm the platform state is still healthy.
